# PDF标注工具 sidecar 依赖
# PyInstaller 打包用 — Windows x86_64

# PDF 处理
PyMuPDF==1.27.2.3

# 图像处理
numpy>=2.0,<3.0
Pillow>=12.0

# OCR (Windows 兼容版本)
paddlepaddle==3.0.0
paddleocr>=3.7,<4.0

# Web 服务
fastapi>=0.115
uvicorn>=0.32

# multipart 支持 (UploadFile)
python-multipart>=0.0.12
